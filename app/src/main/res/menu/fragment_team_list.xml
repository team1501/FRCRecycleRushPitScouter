<?xml version="1.0" encoding="utf-8"?>

<menu xmlns:android="http://schemas.android.com/apk/res/android"
      xmlns:ci="http://schemas.android.com/apk/res-auto">

<item android:id="@+id/menu_item_info"
        android:icon="@drawable/ic_launcher"
        android:title="@string/help"
        ci:showAsAction="ifRoom|withText" />
<item android:id="@+id/menu_item_new_team"
    android:icon="@android:drawable/ic_menu_add"
        android:title="@string/new_team"
        ci:showAsAction="ifRoom|withText"/>
<item android:id="@+id/menu_item_send_teams"
    android:icon="@android:drawable/ic_menu_send"
    android:title="@string/send_teams"
    ci:showAsAction="ifRoom"/>
</menu>